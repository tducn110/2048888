PHASE 1 — PAUSE TRIGGER AUDIT

PAUSE STATE
- existing: YES
- source: `useWinkIntegration` (the `hostPaused` state) and `useGameAudio` (audio pausing).
- pause function: `setHostPaused(true)`
- resume function: `setHostPaused(false)` (via Wink onResume)

CURRENT TRIGGERS
- manual: None locally.
- visibilitychange: Only audio is paused (in `useGameAudio.ts`), game input is not disabled.
- blur: None.
- Wink onPause: Properly sets `hostPaused = true`.

BEHAVIOR
- tab hidden: FAIL (Audio pauses, but game input is not blocked, game is not fully paused).
- focus lost: FAIL.
- Wink parent pause: PASS.
- focus restored: NOT APPLICABLE (No local focus listener).
- Wink resume: PASS (Resumes via Wink contract).

RISKS
- duplicate pause: `useGameAudio.ts` has its own `visibilitychange` listener which might duplicate or conflict if we centralize the pause state.
- auto-resume bug: `useGameAudio.ts` auto-resumes audio when visibility is restored if `!globalHostPaused`. If we centralize it, we must ensure it doesn't auto-resume unless explicitly instructed by Wink `onResume`.
- listener duplication: Need to clean up the standalone `visibilitychange` in `useGameAudio.ts` and consolidate.

CONFIRMED ISSUE: YES

FILES DIRECTLY INVOLVED:
- src/app/App.tsx
- src/hooks/useGameAudio.ts
- src/integrations/wink/useWinkIntegration.ts

FILES ALLOWED TO EDIT:
- src/app/App.tsx
- src/hooks/useGameAudio.ts
- src/integrations/wink/useWinkIntegration.ts

FILES NOT TO TOUCH:
- src/components/game/Game2048.tsx
