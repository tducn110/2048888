PAUSE TRIGGER RESULT

ROOT CAUSE:
- Triggers for window blur were completely missing, and `visibilitychange` was handled disjointly in `useGameAudio` only for audio without syncing with the game's actual pause mechanism (`hostPaused`).

FILES CHANGED:
- src/hooks/useGameAudio.ts (removed disjoint visibilitychange listener)
- src/integrations/wink/useWinkIntegration.ts (added centralized blur and visibilitychange listeners)

PAUSE STATE:
- reused existing state: YES (`hostPaused`)

TRIGGERS:
- manual: N/A
- tab hidden: Triggers `handlePause` (sets `hostPaused = true`)
- window blur: Triggers `handlePause` (sets `hostPaused = true`)
- Wink onPause: Triggers `handlePause` (sets `hostPaused = true`)

RESUME:
- focus restored: STAYS PAUSED (No auto-resume)
- visibility restored: STAYS PAUSED (No auto-resume)
- Wink onResume: Resumes game (sets `hostPaused = false`, adhering strictly to Wink contract)

SAFETY:
- duplicate pause: SAFE (state updates are idempotent, disjoint pause states removed)
- duplicate listeners: SAFE (removed from audio hook, unified centrally)
- cleanup: SAFE (DOM listeners push cleanup functions to `cleanups` array inside `useEffect`)
- gameplay timer/state preserved: SAFE (reuses the exact `hostPaused` logic expected by existing architecture)

VERIFICATION:
- typecheck: PASS
- tests: PASS
- build: PASS

CODE PASS:
PASS

MANUAL ACCEPTANCE:
NOT VERIFIED
